import { AutofocusDirective } from './autofocus.directive';
import { ClickoutDirective } from './clickout.directive';
import { ClipboardDirective } from './clipboard.directive';
import { GravatarDirective } from './gravatar.directive';
import { SortableTableDirective, SortableColumnComponent } from './sortable-table.directive';
// import { DisableIfUnauthorizedDirective, HideIfUnauthorizedDirective } from './auth.directive';

export const directives = [
    AutofocusDirective,
    ClickoutDirective,
    ClipboardDirective,
    GravatarDirective,
    SortableTableDirective,
    SortableColumnComponent,
    // DisableIfUnauthorizedDirective,
    // HideIfUnauthorizedDirective,
];
